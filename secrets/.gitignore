# Everything in this directory is secret by default: certificates (.p12),
# provisioning profiles, App Store Connect API keys (.p8) and the local
# config.mk. Only the templates below are tracked.
*
!.gitignore
!README.md
!config.mk.example
